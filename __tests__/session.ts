/*
Test file for testing all mock endpoint functionality, including:
  - valid login redirect
  - invalid login handling?
  - valid user created for new usr
  - session created & valid
  - session expiration
  - **generation, validation and revocation of access token
*/

describe('session test suite', () => {
  it('will fail a dummy test', () => {
    expect(true).toEqual(false)
  })
})